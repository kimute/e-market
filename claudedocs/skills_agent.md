정확합니다! 매우 좋은 이해이십니다. 명확하게 정리해드리겠습니다.

## Skills vs 에이전트 vs 커스텀 커맨드

### 1. **Skills = 참고 자료 / 지식 베이스**

```
Skills는 "어떻게 해야 하는지"를 알려주는 문서
→ 실행 주체가 아님
→ 에이전트나 Claude가 읽고 참고하는 가이드라인
```

**비유:**
- Skills = 요리책 📖
- 에이전트 = 요리사 👨‍🍳
- Claude Code = 주방 🏪

```markdown
# /mnt/skills/user/frontend-react/SKILL.md
이건 그냥 "React를 이렇게 작성하세요" 라는 문서일 뿐
실제로 코드를 작성하는 건 Claude나 에이전트
```

### 2. **에이전트 = 실행 주체 / 작업자**

```python
# 에이전트는 "실제로 일하는 존재"
class FrontendAgent:
    def __init__(self):
        self.skills = [
            "frontend-react.md",  # 참고 자료 1
            "typescript.md",       # 참고 자료 2
        ]
    
    def execute_task(self, task):
        # 스킬을 읽고
        guidelines = self.read_skills()
        
        # 그걸 바탕으로 코드 작성
        code = self.write_code(task, guidelines)
        
        return code
```

**핵심 차이:**
- **Skills**: 수동적, 읽히기만 함
- **에이전트**: 능동적, 실제로 작업 수행

### 3. **커스텀 커맨드와의 비교**

**매우 유사하지만 범위가 다릅니다:**

```bash
# 커스텀 커맨드 (좁고 구체적)
alias deploy="npm run build && docker build && kubectl apply"
→ 특정 명령어 시퀀스를 단축

# Skills (넓고 컨텍스트적)
/mnt/skills/user/deployment/SKILL.md
→ "배포할 때 고려해야 할 모든 것"
   - 빌드 전 체크리스트
   - 환경변수 확인
   - 롤백 전략
   - 모니터링 설정
```

### 4. **실제 사용 흐름 비교**

#### **Option A: Skills 사용 (참고 자료 방식)**

```bash
# 1. Skills만 작성
/mnt/skills/user/api-design/SKILL.md

# 2. Claude에게 요청
"API 엔드포인트를 만들어줘"

# 3. Claude가 자동으로 스킬 읽음
Claude: "api-design 스킬을 확인했습니다... 
         REST 원칙에 따라 작성하겠습니다..."
         
# 4. 코드 생성
Claude가 스킬 가이드라인을 따라 코드 작성
```

#### **Option B: 에이전트 사용 (자동화 방식)**

```python
# 1. 에이전트 정의 (코드로 작성)
class APIAgent:
    def __init__(self):
        self.skills = ["api-design.md"]
    
    def create_endpoint(self, spec):
        # 스킬 읽기
        guidelines = read_skill(self.skills[0])
        
        # 자동으로 코드 생성
        controller = self.generate_controller(spec, guidelines)
        service = self.generate_service(spec, guidelines)
        test = self.generate_test(spec, guidelines)
        
        return {controller, service, test}

# 2. 에이전트 실행
agent = APIAgent()
agent.create_endpoint("POST /users")

# 3. 자동으로 완성
→ 컨트롤러, 서비스, 테스트 파일 모두 생성됨
```

#### **Option C: 커스텀 커맨드 (단순 자동화)**

```bash
# 1. 커맨드 정의
function create_api_endpoint() {
    local endpoint=$1
    
    # 템플릿 복사
    cp templates/controller.ts src/controllers/$endpoint.controller.ts
    cp templates/service.ts src/services/$endpoint.service.ts
    
    # 이름 치환
    sed -i "s/ENDPOINT/$endpoint/g" src/controllers/$endpoint.controller.ts
}

# 2. 사용
create_api_endpoint "users"

# 3. 결과
→ 파일만 생성, 내용은 템플릿 그대로 (지능 없음)
```

### 5. **조합 사용 예시**

**최적의 방식: Skills + 에이전트 조합**

```
┌─────────────────────────────────────────┐
│  Skills (참고 자료)                      │
│  - API 설계 원칙                         │
│  - 에러 핸들링 패턴                      │
│  - 테스트 작성 가이드                    │
└─────────────────────────────────────────┘
              ↓ (참고)
┌─────────────────────────────────────────┐
│  에이전트 (실행 주체)                    │
│                                          │
│  Backend Agent {                         │
│    읽기: skills                          │
│    실행: 코드 작성                       │
│    검증: 린트, 테스트                    │
│  }                                       │
└─────────────────────────────────────────┘
              ↓ (결과)
┌─────────────────────────────────────────┐
│  생성된 코드                             │
│  - user.controller.ts                    │
│  - user.service.ts                       │
│  - user.spec.ts                          │
└─────────────────────────────────────────┘
```

### 6. **언제 무엇을 사용할까?**

| 상황 | 사용 도구 | 이유 |
|------|----------|------|
| "이렇게 작성해야 해" 라는 규칙 정의 | **Skills** | 참고 문서 |
| 반복적인 작업 자동화 | **에이전트** | 지능적 실행 |
| 단순 명령어 단축 | **커스텀 커맨드** | 빠른 실행 |
| 복잡한 워크플로우 | **Skills + 에이전트** | 품질 + 자동화 |

### 7. **실전 예시**

```markdown
# 상황: REST API 개발

## 1단계: Skills 작성 (가이드라인)
/mnt/skills/user/rest-api/SKILL.md
→ "API는 RESTful 원칙을 따르고, 
   에러는 RFC 7807 형식으로,
   인증은 JWT로..."

## 2단계: 에이전트 설정 (자동화)
BackendAgent {
  skills: ["rest-api.md", "typescript.md"]
  tasks: [
    "컨트롤러 생성",
    "서비스 로직 작성",
    "DTO 정의",
    "테스트 작성"
  ]
}

## 3단계: 커스텀 커맨드 (보조)
alias api="claude-code agent backend --task create-api"

## 결과
$ api users
→ Skills를 참고한 에이전트가
→ 고품질의 API 코드를 생성
→ 커맨드 하나로 실행
```

### 8. **핵심 정리**

```
Skills (문서)
  ↓ "이렇게 해야 해"
에이전트 (실행자)
  ↓ "그럼 내가 할게"
코드 (결과물)
  ↓ "완성!"

커스텀 커맨드는 이 과정을 한 줄로 줄이는 것
```

**결론:**
- ✅ **Skills = 참고 자료** (수동적)
- ✅ **에이전트 = 작업자** (능동적)
- ✅ **커스텀 커맨드 ≈ Skills** (용도는 비슷하지만 범위가 다름)
  - 커스텀 커맨드: 특정 명령 단축
  - Skills: 포괄적인 작업 가이드

명확하게 이해되셨나요? 추가 질문 있으시면 말씀해주세요!